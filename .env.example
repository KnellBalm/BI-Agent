# BI-Agent Environment Variables Template
# Copy this file to .env and fill in the values

# Google Gemini API Keys
# 1. 단일 키 사용 시: GEMINI_API_KEY=...
# 2. 멀티 키(Free/Paid 병용) 사용 시: GEMINI_API_KEYS=key1,key2,key3
# Quota Manager가 할당량을 자동으로 추적하고 로테이션합니다.
GEMINI_API_KEY=your_primary_gemini_api_key_here
GEMINI_API_KEYS=key1,key2,key3

# Google Gemini Model (Default: gemini-2.0-flash)
GEMINI_MODEL=gemini-2.0-flash

# PostgreSQL Connection (Docker: bi-agent-postgres)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=biagent_test
DB_USER=biagent
DB_PASSWORD=biagent123

# MySQL Connection (Docker: bi-agent-mysql, port 3307)
# MYSQL_HOST=localhost
# MYSQL_PORT=3307
# MYSQL_DB=biagent_test
# MYSQL_USER=biagent
# MYSQL_PASSWORD=biagent123

# Docker DB 실행: cd /mnt/z/GitHub/docker-db && docker compose up -d
